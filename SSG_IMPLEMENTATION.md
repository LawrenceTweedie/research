# Реализация полного SSG для страниц рынков с региональными вариациями

## Обзор

Реализована полная статическая генерация (SSG) для всех страниц рынков и их региональных вариаций с оптимизацией для SEO и быстрого рендеринга.

## Что было сделано

### 1. API Endpoints для агрегации данных

Созданы два оптимизированных API endpoints:

#### `/server/api/market/[id].get.ts`
- Загружает данные рынка для всей России
- Агрегирует: activities, news, companies, metrics, available regions
- Один запрос вместо 5-6 отдельных fetch()

#### `/server/api/market/[id]/[regionId].get.ts`
- Загружает региональные данные рынка
- Включает: региональные компании (топ-10), региональные метрики
- Использует файлы `{id}_regions_top10.json` и `{id}_region.json`

### 2. Переработка страниц с useAsyncData()

#### До (проблема):
```javascript
// ❌ Плохо для SEO - данные загружаются на клиенте
onMounted(async () => {
  const data = await fetch('/data/...')
  // HTML генерируется БЕЗ данных
  // Поисковые боты не видят контент!
})
```

#### После (решение):
```javascript
// ✅ Отлично для SEO - данные встраиваются в HTML
const { data: response } = await useAsyncData(
  `market-${marketId.value}`,
  () => $fetch(`/api/market/${marketId.value}`)
)
// HTML генерируется С данными
// Поисковые боты видят весь контент!
```

### 3. Оптимизация Nuxt Config

```typescript
nitro: {
  prerender: {
    crawlLinks: false,        // Явная генерация маршрутов
    concurrency: 50,          // 50 страниц параллельно
    retry: 3,                 // Повтор при ошибках
    retryDelay: 1000
  },
  compressPublicAssets: {     // Сжатие для продакшена
    gzip: true,
    brotli: true
  },
  minify: true
},

experimental: {
  payloadExtraction: true,    // Уменьшение размера HTML
  renderJsonPayloads: true
}
```

### 4. SEO Метатеги

Каждая страница теперь имеет динамические SEO метатеги:

```javascript
useHead({
  title: `${marketName} - Анализ рынка России | INRESEARCH`,
  meta: [
    { name: 'description', content: `...` },
    { name: 'keywords', content: `...` },
    { property: 'og:title', content: `...` },
    { property: 'og:description', content: `...` },
    { name: 'robots', content: 'index, follow' }
  ]
})
```

## Результаты

### Статистика генерации

```
📊 Генерируется страниц:
   - Главная страница: 1
   - Страницы рынков (Россия): 201
   - Региональные страницы: 10,475
   - Всего страниц: 10,677
```

### Преимущества для SEO

1. **Весь контент в HTML** - поисковые боты видят:
   - Названия рынков
   - Финансовые метрики (объем, рентабельность)
   - Топ-10 компаний
   - Новости и источники
   - Виды деятельности
   - Графики и показатели

2. **Мета-теги** - каждая страница имеет уникальные title, description, keywords

3. **Структурированные URL**:
   - `/{marketId}` - рынок по всей России
   - `/{marketId}/{regionId}` - рынок в конкретном регионе

4. **robots.txt friendly** - все страницы помечены как "index, follow"

### Производительность

1. **Параллельная генерация** - 50 страниц одновременно
2. **Оптимизированный payload** - данные извлекаются в отдельные файлы
3. **Compression** - gzip и brotli для статических файлов
4. **Минификация** - HTML/CSS/JS минифицированы

## Использование

### Разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Dev сервер доступен на `http://localhost:3000/`

### Генерация статического сайта

```bash
# Подготовка данных
npm run prepare-data

# Генерация всех 10,677 страниц
npm run generate
```

Генерация займет примерно:
- С concurrency=50: ~5-10 минут
- С concurrency=20: ~15-20 минут
- С concurrency=10: ~30-40 минут

### Просмотр сгенерированного сайта

```bash
npm run preview
```

## Структура файлов

```
research/
├── server/
│   └── api/
│       └── market/
│           ├── [id].get.ts              # API для рынка (Россия)
│           └── [id]/
│               └── [regionId].get.ts    # API для региона
├── pages/
│   ├── index.vue                        # Главная страница
│   └── [marketId]/
│       ├── index.vue                    # Страница рынка (Россия)
│       └── [regionId].vue               # Региональная страница
├── public/
│   └── data/                            # JSON данные
├── nuxt.config.ts                       # Конфигурация SSG
└── SSG_IMPLEMENTATION.md               # Эта документация
```

## API Эндпоинты

### GET /api/market/:id

Возвращает данные рынка для всей России.

**Пример запроса:**
```
GET /api/market/100
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "marketId": "100",
    "marketName": "Название рынка",
    "activities": ["Вид деятельности 1", ...],
    "news": [{"title": "...", "link": "..."}],
    "newsEmotion": "положительно",
    "companies": [{"Название компании": "", "ИНН": "", ...}],
    "metrics": {"Объем рынка 2024": 1000000, ...},
    "availableRegions": [["77", "Г.Москва"], ...]
  }
}
```

### GET /api/market/:id/:regionId

Возвращает данные рынка для конкретного региона.

**Пример запроса:**
```
GET /api/market/100/77
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "marketId": "100",
    "marketName": "Название рынка",
    "regionId": "77",
    "regionName": "Г.Москва",
    "activities": [...],
    "news": [...],
    "newsEmotion": "положительно",
    "companies": [...],  // Топ-10 компаний в Москве
    "metrics": {...},    // Метрики по Москве
    "availableRegions": [...]
  }
}
```

## Проверка SEO

### 1. Проверка HTML контента

После генерации проверьте любой `.html` файл в `.output/public/`:

```bash
cat .output/public/100/index.html
```

Вы должны увидеть:
- ✅ Название рынка в `<title>`
- ✅ Мета-теги с description
- ✅ Весь контент (метрики, компании, новости) в HTML
- ✅ НЕ пустые секции

### 2. Проверка с помощью curl

```bash
curl http://localhost:3000/100 | grep "Объем рынка"
```

Если видите текст "Объем рынка" и значения - SEO работает!

### 3. Google Search Console

После деплоя:
1. Добавьте сайт в Google Search Console
2. Используйте "URL Inspection" для проверки индексации
3. Проверьте, что Googlebot видит контент

## Оптимизация для продакшена

### 1. Уменьшение количества страниц (опционально)

Если генерация слишком долгая, можно:

```typescript
// nuxt.config.ts
// Генерировать только топ-50 регионов для каждого рынка
const topRegions = ['77', '78', '50', ...] // Топ регионы
if (topRegions.includes(regionId)) {
  routes.push(`/${marketId}/${regionId}`)
}
```

### 2. Кэширование при генерации

Nuxt автоматически кэширует API ответы во время генерации.

### 3. CDN

Рекомендуется деплой на:
- Vercel
- Netlify
- Cloudflare Pages

Все поддерживают Nuxt SSG out-of-the-box.

## Troubleshooting

### Проблема: Генерация слишком медленная

**Решение:**
- Уменьшите `concurrency` до 20-30
- Или увеличьте до 100 (если сервер мощный)

### Проблема: Ошибки при генерации

**Решение:**
1. Проверьте логи: `npm run generate 2>&1 | tee generate.log`
2. Убедитесь, что все JSON файлы корректны
3. Увеличьте `retry` в `nuxt.config.ts`

### Проблема: Боты не видят контент

**Решение:**
1. Убедитесь, что используете `npm run generate`, а не `npm run build`
2. Проверьте HTML файл напрямую (см. "Проверка SEO")
3. Убедитесь, что `useAsyncData` вызывается на top-level (не в `onMounted`)

## Заключение

Реализация полного SSG обеспечивает:

✅ **100% SEO-friendly** контент - все данные в HTML
✅ **Быстрая загрузка** - статические файлы
✅ **Оптимизированная генерация** - параллельная обработка 50 страниц
✅ **Масштабируемость** - 10,677 страниц без проблем
✅ **Поддержка регионов** - каждый регион имеет свою страницу

Все страницы теперь полностью готовы к индексации поисковыми системами!
